# 🔥 نظام كشف الحرائق بالذكاء الاصطناعي

نظام بسيط للكشف عن الحرائق باستخدام كاميرا الويب والذكاء الاصطناعي (YOLOv8).

## 📁 الملفات المطلوبة

قبل تشغيل المشروع، تأكد من وجود الملفات التالية في نفس المجلد:

- `main.py` - ملف الكود الرئيسي
- `fire.pt` - نموذج الذكاء الاصطناعي (موجود في الملفات المضغوطة)
- `s.mp3` - ملف صوت الإنذار

## 📦 خطوات التشغيل

### 1. فك ضغط الملفات
يوجد ملفان مضغوطان:
- `fire.part1.rar`
- `fire.part2.rar`

**مهم جداً:** يجب فك ضغط هذين الملفين للحصول على ملف `fire.pt` الذي يحتوي على نموذج الذكاء الاصطناعي المدرب على كشف الحرائق.

استخدم WinRAR أو أي برنامج لفك الضغط، ثم ضع ملف `fire.pt` في نفس مجلد المشروع.

### 2. تثبيت المتطلبات

```bash
pip install ultralytics opencv-python pygame pillow
```

### 3. تشغيل البرنامج

```bash
python main.py
```

## 🎯 طريقة الاستخدام

1. **اختر الكائنات**: من القائمة، حدد أنواع الحرائق أو الدخان التي تريد أن ينبهك البرنامج عند اكتشافها
2. **اضغط Start Detection**: لبدء الكشف المباشر من الكاميرا
3. **الإنذار التلقائي**: عند اكتشاف الحريق، سيظهر تنبيه باللون الأحمر وسيتم تشغيل صوت الإنذار تلقائياً

## ⚠️ ملاحظات مهمة

- تأكد من أن كاميرا الويب متصلة وشغالة
- يجب وجود ملف `fire.pt` بعد فك الضغط في نفس مجلد المشروع
- يجب وجود ملف `s.mp3` للصوت
- الملفات المضغوطة (`fire.part1.rar` و `fire.part2.rar`) يجب فك ضغطهما أولاً قبل تشغيل البرنامج
```
